'use strict';

app.controller('BookController', function ($scope) {
  //$scope.book = Book.get($routeParams.bookId);

  var vm = this;

  activate();
  function activate(){
    
  }

  vm.edit = function(){
  	
  };
});